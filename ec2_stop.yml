---

- hosts: all
  become: true
  tasks:
  - name: print all ansible gatherd ansible_facts
    ansible.builtin.debug:
      var: ansible_facts

  - name: stop EC2 instances
    ansible.builtin.command: /sbin/shutdown -t now 
    when:
      - ansible_facts['os_family'] == "debian"